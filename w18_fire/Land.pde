class Land {
  
 int state;
 int nextState;
 int landSize;
 color landColor;
 int x, y;
 
 //constructor
 Land() {
   // reset ();
   landColor = GRASS_COLOR;
 }
 
 void updateNextState () { // set nextState based on rules
 }
 
 display () {
   
 }
 
 void changeState () { //set state to next state
 }
}
